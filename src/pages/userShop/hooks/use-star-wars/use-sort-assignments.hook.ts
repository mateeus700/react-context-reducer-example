const useStarWarsHook = () => {
  // starWarRequest();
  // try {
  //   const resposta = await service;
  //   starWarSuccess(resposta);
  // } catch (error) {
  //   starWarError(error);
  // }
};

export { useStarWarsHook };
